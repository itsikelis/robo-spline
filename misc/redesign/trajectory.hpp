#include <cstdint>

#include "hermite_spline.hpp"

namespace rspl {
    template <size_t _D>
    class Trajectory {
    public:
        Trajectory(/* args */);
        ~Trajectory();
    };

} // namespace rspl